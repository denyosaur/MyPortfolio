import styled, { keyframes } from 'styled-components';



const TextWrapper = styled.div`
  font-size: 1.5em;

  animation: 2s slide-right 2s forwards;

`;

export default TextWrapper;
// animation: ${slideIn} .5s ease-in 1;